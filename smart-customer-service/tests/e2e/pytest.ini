# E2E 测试配置

[pytest]
testpaths = .
python_files = test_*.py
python_classes = Test*
python_functions = test_*
asyncio_mode = auto
addopts = -v --tb=short
timeout = 60

# 环境变量
env =
    TEST_BASE_URL=http://localhost:8000
    TEST_API_KEY=test-service-key-12345
    TEST_TIMEOUT=30000